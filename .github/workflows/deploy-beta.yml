name: Validate Isolated Cloud Beta
on:
  push:
    branches: [codex/cloud-beta]
    paths: ['beta/**', '.github/workflows/deploy-beta.yml']
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: cloud-beta-validation
  cancel-in-progress: false
jobs:
  validate-beta:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Test primary and beta
        run: |
          python3 -m unittest discover -s tests -v
          python3 -m unittest discover -s beta/tests -v
          node --test beta/tests/model.test.mjs
          bash -n beta/deploy/install.sh beta/deploy/refresh.sh beta/deploy/publish.sh
