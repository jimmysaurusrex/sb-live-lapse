__SITE_ADDRESS__ {
	root * __PUBLISH_ROOT__/current
	encode zstd gzip

	@live path /station_state.json /station_history.json /sba_wwtemp_chart.svg /sba_wwtemp_chart_metric.svg /sba_wwtemp_chart_imperial.svg /healthz.json /snapshots/*
	header @live Cache-Control "no-store"

	@html path / /index.html
	header @html Cache-Control "no-cache"

	header {
		X-Content-Type-Options "nosniff"
		Referrer-Policy "strict-origin-when-cross-origin"
	}

	file_server
}
