__SITE_ADDRESS__ {
	root * __PUBLISH_ROOT__/current
	encode zstd gzip

	@live path /station_state.json /station_history.json /sba_wwtemp_chart.svg /sba_wwtemp_chart_metric.svg /sba_wwtemp_chart_imperial.svg /healthz.json /snapshots/*
	header @live Cache-Control "no-store"

	@html path / /index.html
	header @html Cache-Control "no-cache"
	header @html Content-Security-Policy "default-src 'self'; base-uri 'self'; object-src 'none'; frame-ancestors 'none'; form-action 'none'; script-src 'self'; style-src 'self'; img-src 'self'; connect-src 'self'; upgrade-insecure-requests"

	@page_assets path /app.js /styles.css
	header @page_assets Cache-Control "no-cache"

	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		X-Content-Type-Options "nosniff"
		X-Frame-Options "DENY"
		Referrer-Policy "strict-origin-when-cross-origin"
		Permissions-Policy "accelerometer=(), camera=(), geolocation=(), gyroscope=(), magnetometer=(), microphone=(), payment=(), usb=()"
		Cross-Origin-Opener-Policy "same-origin"
		Cross-Origin-Resource-Policy "same-origin"
	}

	file_server
}
